# WG BULK PACK — Units 11–20

This repo provides MCP configuration patterns and offline-friendly artifacts to support downstream agents.

## Additions in Units 11–20
- `docs/MCP_ADAPTER_INTERFACE.md` — normalized MCP output interface
- `stubs/` + `docs/STUB_MODE.md` — deterministic stub payloads for offline review
- `docs/SECURITY_PROMPTING.md` — prompt-injection hardening + no-fabrication guidance
- Standard PR/issue templates + changelog/release docs
- Single-command verification (`npm run verify`)

## Verify (offline)
```bash
npm run verify
```
